<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sysware.system.mapper.SysDictDataMapper">

    <resultMap type="SysDictData" id="SysDictDataResult">
        <id property="dictCode" column="dict_code"/>
        <result property="sort" column="sort"/>
        <result property="dictLabel" column="dict_label"/>
        <result property="dictValue" column="dict_value"/>
        <result property="dictType" column="dict_type"/>
        <result property="cssClass" column="css_class"/>
        <result property="listClass" column="list_class"/>
        <result property="isDefault" column="is_default"/>
        <result property="status" column="status"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="getSysTable">
        select table_name value,
         CASE
           WHEN table_comment != '' THEN table_comment
           ELSE table_name
         END label

        from information_schema.tables
        where table_schema = (select database()) AND  (table_name  LIKE 'hdy_%' or table_name  LIKE 'sys_%' or table_name  LIKE 'wf_%')

        order by table_name
    </sql>



    <select id="selectSysTableInfo" resultType="java.util.Map">
       select t.* from (<include refid="getSysTable"></include>) t
    </select>

    <select id="getSysTableField" parameterType="String" resultType="java.util.Map">
        select column_name value,column_comment label
        from information_schema.columns where table_schema = (select database()) and table_name = '${tableName}'
        order by ordinal_position
    </select>


</mapper> 