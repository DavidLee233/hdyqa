<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sysware.system.mapper.SysUserDeptMapper">


    <sql id="selectUserVo">
        select u.user_id ,
               u.user_name ,
               u.login_name ,
               u.del_flag ,
               u.leader_type,
               u.remark,
               u.dept_id,
               ss.security_name
        from sys_user u
                 left join sys_security ss on ss.security_id = u.security_id
        where u.del_flag ='0' and u.user_type = '0'
        order by  u.leader_type desc

    </sql>

    <select id="selectMapsPage" resultType="java.util.Map">
        select t.* from (<include refid="selectUserVo"/>) t
        ${ew.customSqlSegment}
    </select>
</mapper>
